name: shoely_dashboard
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.10.1

dependencies:
  cloud_firestore: ^6.1.0
  dartz: ^0.10.1
  device_preview: ^1.3.1
  dio: ^5.9.0
  firebase_core: ^4.3.0
  firebase_storage: ^13.0.5
  flutter:
    sdk: flutter
  flutter_bloc: ^9.1.1
  get_it: ^9.2.0
  go_router: ^17.0.1
  image_picker: ^1.2.1
  modal_progress_hud_nsn: ^0.5.1
  path: ^1.9.1
  skeletonizer: ^2.1.2
  supabase_flutter: ^2.12.0

dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
